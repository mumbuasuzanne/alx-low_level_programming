#include<stdio.h>
/**
 * main - A program that print a line using a printf function
 * Return: 0 (Success)
 */
int main(void)
{
        printf("with proper grammar, but the outcome is a apiece of art,\n");
        return (0);
}
